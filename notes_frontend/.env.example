# PUBLIC INTERFACE - Environment variables for Notes Frontend
# Base URL of the backend API (e.g., http://localhost:8000 or https://api.example.com)
NUXT_PUBLIC_API_BASE=

# Optional: Base URL of the site (used for auth redirects if needed)
NUXT_PUBLIC_SITE_URL=
