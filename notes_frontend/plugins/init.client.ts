export default defineNuxtPlugin(() => {
  // This plugin exists to ensure client-side initialization occurs early.
  // Useful if any client-only logic is needed in the future.
  // Currently, no-op.
})
